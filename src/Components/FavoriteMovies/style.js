import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 76,
    height: 95,
    borderRadius: 10,
    marginEnd: 13,
    marginBottom: 18,
  },
});

export default styles;
